HELP INTERSECT                                        A. Sloman Dec 1982

    intersect(a, b, c, d) -> p

If A, B, C, D are points represented by two element lists, this returns the
point P which is the intersection of the lines AB and CD.

A mishap (dividing by 0) occurs if the lines are parallel.
