HELP ADD                                         Steven Hardy, September 1979

This procedure is used to add an item to the database. It could be defined as

    define add(item);
      item -> it;
      item :: database -> database
    enddefine;

See also HELP * PRESENT, * LOOKUP, * REMOVE, * FLUSH, * MATCH, * IT.
